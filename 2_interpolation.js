// 02. Declare variables to store your first name, last name, marital status, country and age and display them using interploation method.

// Ans: interpolation method is used with literals ${}

const firstName = "Akshay";
const lastName = "Kute";
let maritalStatus ='Single';
let country = 'India';
let age = 24 ; 

console.log(` My name is ${firstName} ${lastName}.
 I am ${age} years old. 
 currently I am ${maritalStatus}.
 I live in ${country}.`);